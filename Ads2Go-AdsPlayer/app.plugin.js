const { withAndroidManifest } = require('@expo/config-plugins');

/**
 * Expo config plugin to ensure proper Android configuration
 */
module.exports = function withAndroidConfigPlugin(config) {
  return config;
};


